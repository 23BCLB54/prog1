def calculate_simple_interest(principal, rate, time):
    interest = (principal * rate * time) / 100
    return interest

# Function call
si = calculate_simple_interest(10000, 5, 2)
print("Simple Interest is:", si)
